spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL5Dialect

  mvc:
    view:
      suffix: .html

